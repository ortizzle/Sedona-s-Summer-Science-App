{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] && git add \"$f\" && git diff --cached --stat | head -1 | { read -r summary; git commit -m \"Update $(basename \"$f\"): $summary\" 2>/dev/null || true; }; } 2>/dev/null || true",
            "statusMessage": "Committing changes..."
          }
        ]
      }
    ]
  }
}
